# CORS

## Same-origin policy

The same origin policy checks the scheme/host/port triple, i.e. HTTP
or HTTPS, hostname and port (if it's specified explicitly).

That means that an origin of `http://x.com` is different from
`https://x.com`, for example.
